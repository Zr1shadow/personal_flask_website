# from routing import db

# class MangaDataBase(db.Model):
#     id = db.Column(db.Integer, primary_key=True)
#     title = db.Column(db.String)
#     chapters = db.Column(db.JSON, nullable = False)
#     # chapter_link = db.Column(db.JSON, nullable = False)
    
#     def __repr__(self):
#         return f"Manga('{self.title}', '{self.chapters}')"